name: ci

on:
  pull_request:
    branches: [ master ]

  workflow_dispatch:

env:
  IMAGE_NAME: initc3/gramine

jobs:
  linux-sgx:
    strategy:
      matrix:
        version: [2.13.3, 2.14, 2.15.1, 2.16, 2.17.1, 2.18, 2.19]
        distro: [focal]
        include:
          - version: 2.13.3
            distro: bionic
          - version: 2.18
            distro: jammy
          - version: 2.19
            distro: jammy
          - version: 2.19
            distro: buster
        exclude:
          - version: 2.19
            distro: focal

    runs-on: ubuntu-latest

    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Set Short SHA
        id: vars
        run: echo "::set-output name=short_sha::$(git rev-parse --short HEAD)"
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      -
        name: Build docker image for linux-sgx ${{ matrix.version }} ${{ matrix.distro }}
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./${{ matrix.version }}/${{ matrix.distro }}/Dockerfile
          builder: ${{ steps.buildx.outputs.name }}
          tags: |
            initc3/linux-sgx:${{ matrix.version }}-${{ matrix.distro }},
            initc3/linux-sgx:${{ matrix.version }}-${{ matrix.distro }}-${{ steps.vars.outputs.short_sha }}
